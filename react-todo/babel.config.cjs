module.exports = {
  presets: [
    '@babel/preset-env',
    '@babel/preset-react',  // Ensure this is present if you're using JSX
  ],
};
module.exports = {
  presets: [
    '@babel/preset-env',   // For transforming ES6+ syntax
    '@babel/preset-react'  // For transforming JSX code
  ]
 
};
